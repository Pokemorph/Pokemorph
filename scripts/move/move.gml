/* Name: Move_list
* Description: List of moves for programing
* @desc move(move_name, move_desc, move_power, move_type, num_hit, target, effect)
* @param move_id id of move
* @param move_name name of move
* @param move_desc description of move
* @param move_power attack power
* @param move_type move type ie grass, fire, water, ext...
* @param num_hits how many times the move can hit
* @param target target you, your allies, your enemy, all enemys, everyone
* @param effect extra effects like poison, regen, paralize, ext...
*/

var move_name, move_desc, move_power, move_type, num_hit, target, effect;

move_name = argument0;
move_desc = argument1;
move_power = argument2;
move_type = argument3;
num_hit = argument4;
target = argument5;
effect = argument6;
